# QA-3 — Fix Audit + Full Code QA (2026-06-06)

Audit of every fix from QA-1 (internal) and the Anna customer-QA rounds, plus a
full code-level QA pass across the monorepo. Run by Claude.

---

## Part 1 — QA-1 internal bugs: all resolved

| Bug | Severity | Status | Evidence |
|-----|----------|--------|----------|
| BUG-1 — approval_snapshots not immutable | HIGH | ✅ Fixed | `migration 040` (select+insert only). **Regression found & fixed today — see below.** |
| BUG-2 — free-plan breaker threshold dead code | HIGH | ✅ Fixed | `pricing.ts` `breakerState()` uses `PLAN_PRICING[plan].breakerFreezePct` |
| BUG-3 — CRON_SECRET hardcoded `'vigmis-cron'` | BLOCKER | ✅ Fixed | API: `middleware/secrets.ts` fail-closed; web cron routes return 500 if unset, no fallback |
| BUG-4 — missing tsconfig in 2 packages | MED | ✅ Fixed | `ad-connectors/tsconfig.json`, `ai-router/tsconfig.json` present; both typecheck clean |
| BUG-5 — crons sent empty body | MED | ✅ Fixed | all web cron routes send `body: '{}'` |
| BUG-6 — 3 routes not deployed | HIGH | ✅ Fixed | routes registered in `server.ts`; deployed via current Railway main |

## Regression found & fixed during this audit
**Migration 045 (RLS lockdown) re-opened UPDATE/DELETE on append-only audit tables** by adding a
blanket `tenant_isolation_all` (FOR ALL) policy — undoing BUG-1's intent.
**Fixed: migration 046** — `approval_snapshots, content_attestations, audit_log, content_decisions,
bypass_attempts` are now INSERT+SELECT only. **Verified live:** all 5 expose only INSERT,SELECT policies.
(Service-role bypasses RLS, so the API is unaffected.)

## Part 2 — Anna customer-QA rounds: present & compiling
Commits `0dd9663`, `c675469`, `7283126` (strategy depth, media posts, brand assets, autofocus,
post preview, AI image gen, Google account error handling, platform preference, Creative library).
All present in current code and compile clean (symbols verified: brand_assets, platform preference,
post preview/image gen, autoFocus, Creative library).

## Part 3 — Full code-level QA (this pass)

| Check | Result |
|-------|--------|
| `apps/api` typecheck (`tsc --noEmit`) | ✅ EXIT 0 |
| `apps/web` typecheck | ✅ EXIT 0 |
| `packages/ad-connectors` typecheck | ✅ EXIT 0 |
| `packages/ai-router` typecheck | ✅ EXIT 0 |
| API boots + all routes register | ✅ (booted on a free port; `/health` 200) |
| Security headers live | ✅ (X-Frame-Options, HSTS, nosniff present on responses) |
| Security pass (Phase 1) | ✅ deployed (RLS, secrets, HMAC, headers, log redaction) |

**Conclusion:** the codebase is green — everything typechecks, builds, and boots; all documented
QA fixes are in place; and a latent RLS regression was caught and fixed.

---

## ⚠️ Honest limits of this QA (what was NOT done)
- **No automated tests exist** in the repo (unit/integration/e2e). All functional QA is manual.
- **Live end-to-end flows were NOT exercised** here — they need real third-party accounts + a browser:
  Clerk sign-up, Google/Meta/TikTok OAuth, real AI generation, social publishing, Paddle/Stripe billing,
  GA4 sync, comment fetch/reply. Code is wired correctly, but "it runs in production" must be confirmed
  by walking the `qa/QA-2-customer-ana.md` script against live accounts.
- Recommended next step for true coverage: add a small automated test suite (at least API handler
  unit tests + one happy-path e2e), since manual QA won't scale and regressions (like the 045→046 one)
  are easy to miss.
